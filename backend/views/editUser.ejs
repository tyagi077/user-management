<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <title>Edit User</title>
    <script src="https://cdn.tailwindcss.com"></script>
  </head>
  <body class="bg-gray-100 p-6">
    <div class="max-w-xl mx-auto bg-white p-6 rounded shadow">
      <h2 class="text-2xl font-semibold mb-4">Edit Role of <%= targetUser.username %></h2>
      <form
        action="/user/update-user/<%= targetUser.id %>"
        method="POST"
        class="space-y-4"
      >
        <label class="block">Role:</label>
        <select name="role" class="w-full p-2 border rounded" required>
          <option value="user" <%= targetUser.role === 'user' ? 'selected' : '' %>>user</option>
          <option value="admin" <%= targetUser.role === 'admin' ? 'selected' : '' %>>admin</option>
        </select>
        <input type="hidden" name="updated_by" value="<%= user.username %>" />
        <button class="bg-blue-500 text-white px-4 py-2 rounded hover:bg-blue-600" type="submit">
          Update
        </button>
      </form>
      <div class="mt-4">
        <a href="/dashboard" class="text-blue-500 underline">← Back to Dashboard</a>
      </div>
    </div>
  </body>
</html>
